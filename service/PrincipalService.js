'use strict';


/**
 * Añade el procesamiento de todos los datos de los sensores para generar una alarma determinada.
 * Añade una alarma al sistema dependiendo de los parámetros
 *
 * body DatosTodosSensores Datos de todos los sensores a insertar (optional)
 * no response value expected for this operation
 **/
exports.addAlarma = function(body) {
  return new Promise(function(resolve, reject) {
    resolve();
  });
}

